#include <stdio.h>
int main()
{
    // *,/,%<--->+,-,<-->=
    //if *,/ ek sath then left to right solve krna he
    int x = 2;
    int y = 3;
    printf("the valu of 8*y/3*x is %d \n",8*y/3*x); //first 8*3 then devide by 3 then multi by 2 --16 not 24/6
    return 0;
}